import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.burak.app',
  appName: 'yemek-dunyasi',
  webDir: 'dist/kitchen-world-web-ui',
  bundledWebRuntime: false
};

export default config;
